[project]
name = "healthcare-mcp-server"
version = "0.0.2"
description = "This server allows you to search for FHIR (Fast Healthcare Interoperability Resources) resources on a specified FHIR server."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.9.1",
    "requests>=2.32.3",
]
