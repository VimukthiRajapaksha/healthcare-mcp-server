[project]
name = "app"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "modelcontextprotocol>=0.1.0",
    "python-dotenv>=1.1.0",
    "requests>=2.32.3",
]

[dependency-groups]
dev = [
    "pytest>=8.3.5",
    "pytest-mock>=3.14.1",
]
